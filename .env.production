# Production defaults (Vercel build uses `vite build` -> production mode)
VITE_API_URL=https://mcd-hrms-api.onrender.com
VITE_ML_SERVICE_URL=https://mcd-hrms-ml.onrender.com
# If you change API_KEY on Render, set the same value in Vercel env vars instead of committing here.
VITE_API_KEY=hackathon-demo-key
